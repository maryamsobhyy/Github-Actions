name: 07-basics first workflow
run-name: ${{github.actor  }} explaning data 

on:
  workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo ${{ toJson(github.event) }} }}
